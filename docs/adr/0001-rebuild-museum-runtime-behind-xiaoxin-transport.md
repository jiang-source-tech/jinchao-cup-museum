# 保留历史设备传输路径，独立运行博物馆业务

历史设备的 `/xiaoxin/v1/`、`/xiaoxin/ota/`、音频链路和模型适配已经与固件形成稳定合同，因此继续作为传输兼容层保留；学生陪伴与博物馆业务的领域模型和不变量完全不同，因此新的业务实现放在独立 `MuseumRuntime` 后面，由通用 `ConversationRuntime` interface 接入连接层，不在旧业务命名空间中继续叠加分支。这样能降低协议迁移风险，同时避免兼容路径继续承载旧业务语义。
