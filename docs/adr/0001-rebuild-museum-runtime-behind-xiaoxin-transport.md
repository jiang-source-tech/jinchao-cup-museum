# 保留小芯设备传输路径，独立重建博物馆业务运行时

小芯的 `/xiaoxin/v1/`、`/xiaoxin/ota/`、音频链路和模型适配已经与固件形成稳定合同，因此继续保留；学生陪伴与博物馆伴游的领域模型和不变量完全不同，因此新的博物馆业务实现放在独立 `MuseumRuntime` 后面，由通用 `ConversationRuntime` interface 接入连接层，不在旧 `core/xiaoxin` 中继续叠加分支。这样能降低协议迁移风险，同时允许在新主链路稳定后完整删除旧业务。
